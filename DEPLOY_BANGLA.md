# ক্লাউডে ডিপ্লয় করার সহজ গাইড (Render) 🚀

এই গাইড অনুসরণ করলে আপনার ওয়েব স্ক্র্যাপার অ্যাপটা ক্লাউডে ২৪/৭ চলবে —
**ল্যাপটপ বন্ধ থাকলেও।** কোনো কোড লিখতে হবে না, শুধু মাউস ক্লিক।
সব কাজ **মোবাইল দিয়েই** করা যাবে।

---

## যা যা লাগবে

- একটা GitHub অ্যাকাউন্ট (আপনার কোড ইতিমধ্যে এখানে আছে)
- একটা Render অ্যাকাউন্ট (ফ্রি) — [render.com](https://render.com)

---

## ধাপ ১ — Render-এ অ্যাকাউন্ট খুলুন

1. মোবাইল/কম্পিউটারের ব্রাউজারে যান: **https://render.com**
2. **"Get Started"** বা **"Sign Up"** এ ক্লিক করুন।
3. **"GitHub দিয়ে সাইন আপ"** বেছে নিন (সবচেয়ে সহজ)।
4. GitHub লগইন করে Render-কে অনুমতি দিন।

---

## ধাপ ২ — অ্যাপটা ডিপ্লয় করুন

1. Render ড্যাশবোর্ডে **"New +"** বাটনে ক্লিক করুন।
2. তালিকা থেকে **"Blueprint"** বেছে নিন।
3. আপনার রিপো **`web-scraper-app`** খুঁজে **"Connect"** করুন।
   - (প্রথমবার হলে "Configure GitHub" করে রিপোতে Render-এর অ্যাক্সেস দিন)
4. Render নিজে থেকেই `render.yaml` ফাইলটা পড়ে সব সেট করে ফেলবে।
5. নিচে **"Apply"** বা **"Create"** বাটনে ক্লিক করুন।
6. ৩–৫ মিনিট অপেক্ষা করুন — Render আপনার অ্যাপ তৈরি করবে (Build → Deploy)।

---

## ধাপ ৩ — অ্যাপ খুলুন

- ডিপ্লয় শেষ হলে উপরে একটা লিংক দেখাবে, যেমন:
  **`https://web-scraper-app.onrender.com`**
- এই লিংকে ক্লিক করলেই আপনার ড্যাশবোর্ড খুলবে। 🎉
- এই লিংকটা **বুকমার্ক** করে রাখুন — মোবাইল/যেকোনো ডিভাইস থেকে খুলতে পারবেন।

---

## এখন কীভাবে ব্যবহার করবেন

1. ড্যাশবোর্ডে একটা **Job Name** দিন (যেমন "Tech News")।
2. যে ওয়েবসাইট স্ক্র্যাপ করবেন তার **URL** দিন।
3. কী কী তুলবেন বেছে নিন (Text / Links / Images)।
4. **"Start Scraping"** এ ক্লিক করুন।
5. ডেটা এলে দেখুন, আর CSV/JSON হিসেবে ডাউনলোড করুন।

---

## গুরুত্বপূর্ণ কিছু কথা (সততার সাথে)

- **ফ্রি প্ল্যান ঘুমিয়ে পড়ে:** ১৫ মিনিট কেউ ব্যবহার না করলে অ্যাপটা "ঘুমিয়ে" যায়।
  আবার লিংকে ঢুকলে ৩০–৫০ সেকেন্ডে জেগে ওঠে। এটা স্বাভাবিক।
  আপনি যখনই ঢুকে জব দেবেন, তখনই কাজ করবে — ল্যাপটপ লাগবে না।

- **ডেটা মুছে যেতে পারে:** ফ্রি প্ল্যানে স্থায়ী ডিস্ক নেই, তাই অ্যাপ রিস্টার্ট/আপডেট
  হলে পুরোনো জবের ডেটা মুছে যেতে পারে। জরুরি ডেটা **CSV/JSON ডাউনলোড** করে রাখুন।
  (পরে চাইলে একটা ফ্রি ডেটাবেস যোগ করে এটা স্থায়ী করা যায় — বললে করে দেব।)

- **JavaScript স্ক্র্যাপিং (Selenium):** ফ্রি সেটআপে Chrome নেই, তাই "Handle
  JavaScript" অপশনটা এখন কাজ করবে না। সাধারণ ওয়েবসাইট এটা ছাড়াই ঠিক চলবে।
  দরকার হলে পরে যোগ করা যাবে।

---

## ভবিষ্যতে কোড বদলাতে চাইলে

- আপনি মোবাইল থেকে **claude.ai/code** এ গিয়ে আমাকে বাংলায় বললেই হবে।
- আমি কোড বদলে GitHub-এ push করব, আর Render **নিজে থেকেই** নতুন ভার্সন লাইভ করে দেবে।

কোনো জায়গায় আটকে গেলে স্ক্রিনশট দিয়ে বাংলায় জিজ্ঞেস করুন — সাহায্য করব। 🙌
